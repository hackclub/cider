import 'package:flutter/material.dart';

class CrosswordPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Crossword")),
      body: Center(child: Text("Crossword puzzle will be implemented here.")),
    );
  }
}
